package config

// DefaultScanPath is the canonical workspace root used by repo-local tooling,
// prompts, and generated client configuration when no explicit scan path is set.
const DefaultScanPath = "~/hairglasses-studio"
