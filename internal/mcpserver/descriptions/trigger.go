package descriptions

const (
	DescRalphglassesTriggerWebhook = "Trigger an agent session from an external source (webhook, API, or another agent). Creates a trigger record and optionally launches immediately."
	DescRalphglassesScheduleCreate = "Create, list, enable, or disable cron-based schedules. When repo is provided, schedules are persisted to that repo's .ralph/schedules/*.json; otherwise the legacy ~/.ralph/schedules.json store is used."
)
