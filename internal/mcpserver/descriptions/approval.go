package descriptions

const (
	DescRalphglassesRequestApproval = "Request human approval for an action — creates a pending record and optionally pauses the session until resolved"
	DescRalphglassesResolveApproval = "Resolve a pending approval — approves or rejects the requested action and resumes the paused session if applicable"
	DescRalphglassesListApprovals   = "List pending approval requests (set include_resolved=true for all)"
)
