#!/bin/bash
# install-to-disk.sh — Install ralphglasses to a target disk
# TODO: Implement disk installation (partition, copy rootfs, install GRUB)
set -euo pipefail

echo "install-to-disk.sh: Not yet implemented"
echo "Usage: sudo install-to-disk.sh /dev/sdX"
exit 1
