{
  "_comment": "Example MCP client configuration for Claude Desktop or Claude Code.",
  "_comment2": "Copy the 'mcpServers.ralphglasses' block into your ~/.claude.json or Claude Desktop config.",

  "mcpServers": {
    "ralphglasses": {
      "_comment": "Option A: Run from source (recommended for development)",
      "command": "go",
      "args": ["run", ".", "mcp"],
      "cwd": "/path/to/ralphglasses",
      "env": {
        "RALPHGLASSES_SCAN_PATH": "~/hairglasses-studio"
      }
    }
  }
}
