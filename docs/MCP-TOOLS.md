# MCP Server Contract

> Auto-generated by `go run ./tools/genmcpdocs`. Do not edit manually.

Ralphglasses exposes **222 tools**: **218 grouped tools** across **30 deferred-load tool groups** plus **4 management tools** that are always available. The server also exposes **3 static resources**, **3 resource templates**, and **5 prompts**.

## Discovery Workflow

- Read `ralph:///catalog/server` for the live server contract.
- Read `ralph:///catalog/tool-groups` for grouped capabilities and counts.
- Read `ralph:///catalog/workflows` for common operator playbooks.
- Call `ralphglasses_tool_groups`, then `ralphglasses_load_tool_group` before invoking non-core grouped tools in deferred mode.

## Management Tools

| Tool | Description |
|------|-------------|
| `ralphglasses_load_tool_group` | Load all tools in a named group (core, session, loop, prompt, fleet, repo, roadmap, team, tenant, awesome, advanced, events, feedback, eval, fleet_h, observability, rdcycle, plugin, sweep, rc, autonomy, workflow, docs, recovery, promptdj, a2a, trigger, approval, context, prefetch). Use ralphglasses_tool_groups or ralph:///catalog/tool-groups first if you need discovery. |
| `ralphglasses_server_health` | Show the active ralphglasses MCP contract shape, including available tool groups, loaded groups, and resource/prompt coverage. |
| `ralphglasses_skill_export` | Generate SKILL.md documentation from all registered tool groups. Returns markdown or JSON. |
| `ralphglasses_tool_groups` | List available tool groups for deferred loading. Call ralphglasses_load_tool_group to load a specific group. |

## Tool Groups

| Group | Tools | Description |
|-------|-------|-------------|
| `core` | 20 | Essential fleet management: scan, list, start, stop, pause, logs, config |
| `session` | 19 | LLM session lifecycle: launch, list, status, resume, stop, budget, retry, output, tail, diff, compare, errors, export |
| `loop` | 12 | Perpetual development loops: start, status, step, stop, benchmark, baseline, gates, self-test, self-improve |
| `prompt` | 9 | Prompt enhancement: analyze, enhance, lint, improve, classify, should_enhance, templates, template_fill |
| `fleet` | 12 | Fleet operations: fleet_status, analytics, submit, budget, workers, dlq, marathon_dashboard, capacity_plan |
| `repo` | 9 | Repo management: health, optimize, scaffold, claudemd_check, snapshot |
| `roadmap` | 6 | Roadmap automation: parse, analyze, research, expand, export |
| `team` | 6 | Agent teams and definitions: team_create, team_status, team_delegate, agent_define, agent_list, agent_compose |
| `tenant` | 5 | Workspace tenant administration: list, create, status, rotate trigger token, and batch role leaderboards |
| `awesome` | 5 | Awesome-list research: fetch, analyze, diff, report, sync |
| `advanced` | 5 | Advanced: journals, tool benchmark, circuit breaker reset |
| `events` | 2 | Fleet event bus: query and poll for session, cost, loop, and circuit events |
| `feedback` | 6 | Provider feedback: performance profiles, recommendations, bandit stats, confidence calibration |
| `eval` | 7 | Offline evaluation: counterfactual analysis, Bayesian A/B testing, frequentist significance testing, changepoint detection |
| `fleet_h` | 5 | Fleet intelligence: blackboard coordination, A2A task delegation, cost forecasting, cost recommendations |
| `observability` | 14 | Observation queries, scratchpad automation, loop wait/poll, coverage, cost estimation, merge verification |
| `rdcycle` | 16 | R&D cycle automation — finding-to-task conversion, cycle planning, baselines, merging, and scheduling |
| `plugin` | 4 | Plugin management: list, info, enable, disable registered plugins |
| `sweep` | 8 | Cross-repo audit sweeps: generate optimized prompts, fan-out sessions, monitor, nudge stalled sessions, schedule recurring checks |
| `rc` | 6 | Remote control — send prompts, read output, and act on sessions from mobile or scripted contexts |
| `autonomy` | 8 | Autonomy management — view/set autonomy level, inspect supervisor status, review and override autonomous decisions, track HITL events |
| `workflow` | 3 | Workflow automation — define, run, and delete multi-step YAML workflows that sequence agent sessions |
| `docs` | 8 | Docs repo integration: search research, check existing, write findings, push changes, meta-roadmap coordination, cross-repo roadmap management |
| `recovery` | 6 | Emergency session recovery: triage killed sessions, salvage partial output, generate recovery plans, batch re-launch, write incident reports, discover orphaned sessions |
| `promptdj` | 6 | Prompt DJ: quality-aware prompt routing to optimal providers |
| `a2a` | 4 | A2A protocol integration: discover agents, send tasks, check status, export agent card |
| `trigger` | 2 | External agent triggering and cron-based scheduling |
| `approval` | 3 | Human-in-the-loop approval: request, resolve, list pending approvals (Factor 7: Contact Humans with Tool Calls) |
| `context` | 1 | Context window budget monitoring: track token usage per session |
| `prefetch` | 1 | Deterministic context pre-fetching: inspect registered hooks |

## Resources

### Static Resources

| URI | Name | Description |
|-----|------|-------------|
| `ralph:///catalog/server` | Server catalog | Read the live MCP contract: tool counts, tool groups, resources, prompts, and default discovery guidance. |
| `ralph:///catalog/tool-groups` | Tool group catalog | Read the grouped tool catalog with descriptions and tool counts. |
| `ralph:///catalog/workflows` | Workflow catalog | Read the common ralphglasses operator workflows and their discovery entrypoints. |

### Resource Templates

| URI | Name | Description |
|-----|------|-------------|
| `ralph:///{repo}/status` | Repo status | Read .ralph/status.json for a repository. |
| `ralph:///{repo}/progress` | Repo progress | Read .ralph/progress.json for a repository. |
| `ralph:///{repo}/logs` | Repo logs | Read the last 100 lines of .ralph/logs/ralph.log for a repository. |

## Prompts

| Prompt | Description |
|--------|-------------|
| `self-improvement-planner` | Plan a self-improvement iteration for a repository with goals, steps, validation criteria, and rollback strategy. |
| `code-review` | Review code changes in a repository file with structured severity-focused feedback. |
| `test-generation` | Generate a structured test plan for a repository file, including coverage targets and edge cases. |
| `bootstrap-firstboot` | Build a first-boot checklist for bringing a workspace and provider CLIs into a healthy Ralph-ready state. |
| `provider-parity-audit` | Audit provider parity for a repository across AGENTS, provider config, MCP config, generated skills, and prompts. |


## core

Essential fleet management: scan, list, start, stop, pause, logs, config


### `ralphglasses_config`

Get or set .ralphrc config values for a repo

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `key` | string | optional | Config key to get/set (omit to list all) |
| `value` | string | optional | Value to set (omit to get current value) |



### `ralphglasses_config_bulk`

Get/set .ralphrc `key` values across multiple repos

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `key` | string | **required** | Config key to get/set |
| `repos` | string | optional | Comma-separated repo names (default: all) |
| `value` | string | optional | Value to set (omit to query) |



### `ralphglasses_config_schema`

List known config keys with type and constraint metadata

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `include_constraints` | boolean | optional | Include rendered constraints (default: true) |
| `include_defaults` | boolean | optional | Include default metadata when available |
| `key` | string | optional | Optional single key to inspect |



### `ralphglasses_debug_bundle`

Build a sanitized debug bundle matching the CLI debug-bundle workflow

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: view (default) or save |
| `name` | string | optional | Optional output filename when action=save |
| `repo` | string | optional | Optional repo name whose root should anchor the bundle save path |
| `sections` | string | optional | Comma-separated bundle sections to include |



### `ralphglasses_doctor`

Run CLI-style environment and workspace readiness checks: binaries, config, state dir, sqlite, scan path, disk, and API keys

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `checks` | string | optional | Comma-separated check names to run (e.g. git,scan_path,api_keys) |
| `include_optional` | boolean | optional | Include optional provider/API key checks (default: true) |
| `scan_path` | string | optional | Override scan root to inspect (defaults to the server scan path) |



### `ralphglasses_firstboot_profile`

Read, update, validate, or mark done the thin-client firstboot profile

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: get (default), set, validate, or mark_done |
| `anthropic_api_key` | string | optional | Anthropic API key override |
| `autonomy_level` | number | optional | Autonomy level 0-3 |
| `config_dir` | string | optional | Optional config directory override (defaults to ~/.ralphglasses) |
| `coordinator_url` | string | optional | Fleet coordinator URL |
| `google_api_key` | string | optional | Google API key override |
| `hostname` | string | optional | Hostname to persist or validate |
| `openai_api_key` | string | optional | OpenAI API key override |



### `ralphglasses_list`

List all discovered repos with status summary

*No parameters.*



### `ralphglasses_logs`

Get recent log lines from a repo's ralph log

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `lines` | number | optional | Number of lines to return (default 50, max 500) |



### `ralphglasses_pause`

Pause or resume a running ralph loop for a `repo`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name to pause/resume |



### `ralphglasses_scan`

Scan for ralph-enabled repos and return their current status

*No parameters.*



### `ralphglasses_start`

Start a ralph loop for a repo

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name to start loop for |



### `ralphglasses_status`

Get detailed status for a specific repo including loop status, circuit breaker, progress, and config

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name (basename of directory) |
| `include_config` | boolean | optional | Include full config in status response |



### `ralphglasses_stop`

Stop a running ralph loop for a repo

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name to stop loop for |



### `ralphglasses_stop_all`

Stop all running ralph loops

*No parameters.*



### `ralphglasses_tasks_cancel`

Cancel a running async task by `task_id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `task_id` | string | **required** | Task ID to cancel |



### `ralphglasses_tasks_get`

Get status of an async task by `task_id` — poll for long-running operations (loop_start, fleet_submit, self_improve)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `task_id` | string | **required** | Task ID returned from async tool invocation |



### `ralphglasses_tasks_list`

List all async tasks with optional state filter

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `state` | string | optional | Filter by state: running, completed, failed, canceled, input_required |



### `ralphglasses_telemetry_export`

Export local telemetry data as JSON or CSV with optional filtering

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `format` | string | optional | Output format: json (default) or csv |
| `limit` | number | optional | Maximum events to return |
| `provider` | string | optional | Optional provider filter |
| `repo` | string | optional | Optional repo filter |
| `since` | string | optional | RFC3339 or YYYY-MM-DD lower bound |
| `type` | string | optional | Optional telemetry event type filter |
| `until` | string | optional | RFC3339 or YYYY-MM-DD upper bound |



### `ralphglasses_theme_export`

Export a named theme in ghostty, starship, or k9s format

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `format` | string | **required** | Export format: ghostty, starship, or k9s |
| `theme` | string | **required** | Theme name |



### `ralphglasses_validate`

Validate .ralphrc files across one repo or the full scan path

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `include_clean` | boolean | optional | Include OK repos in the response (default: false) |
| `repo` | string | optional | Single repo name to validate |
| `repos` | string | optional | Comma-separated repo names to validate |
| `scan_path` | string | optional | Override scan root to validate (defaults to the server scan path) |
| `strict` | boolean | optional | Treat warnings as errors (default: false) |




## session

LLM session lifecycle: launch, list, status, resume, stop, budget, retry, output, tail, diff, compare, errors, export


### `ralphglasses_budget_status`

Show aggregate budget status across all sessions, matching the CLI budget status command

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_error_context`

Get error context for a session — consecutive errors, escalation status, and formatted recent errors for LLM context injection (12-Factor Agents Factor 9)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `session_id` | string | **required** | Session ID |



### `ralphglasses_session_budget`

Get cost/budget info for a session by `id`, update the budget, or reset spend tracking

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID |
| `action` | string | optional | Action: get (default), set, or reset_spend |
| `budget_usd` | number | optional | Budget in USD — new budget to set (omit to just query) |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_compare`

Compare two sessions by `id1` and `id2`: cost, turns, duration, provider efficiency

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id1` | string | **required** | First session ID |
| `id2` | string | **required** | Second session ID |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_diff`

Git changes made during a session's execution window by `id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID |
| `max_lines` | number | optional | Truncate diff at N lines (default 200) |
| `stat_only` | string | optional | true/false (default: true) |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_errors`

Aggregated error view: parse failures, API errors, budget warnings

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `limit` | number | optional | Max errors (default 50) |
| `repo` | string | optional | Filter by repo name |
| `severity` | string | optional | Filter: critical, warning, info |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_export`

Export a recorded session replay as Markdown or JSON — includes timeline, tool calls, inputs/outputs

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `session_id` | string | **required** | Session ID whose replay to export |
| `after` | string | optional | Only include events after this RFC3339 timestamp |
| `before` | string | optional | Only include events before this RFC3339 timestamp |
| `event_types` | string | optional | Comma-separated event types to include: input, output, tool, status (default: all) |
| `format` | string | optional | Export format: markdown (default) or json |
| `repo` | string | optional | Repo name hint for locating replay file |



### `ralphglasses_session_fork`

Fork a running or completed session — creates a child session with the parent's context and optional new prompt

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Parent session ID to fork from |
| `budget_usd` | number | optional | Budget override in USD (inherits parent budget if omitted) |
| `inject_context` | boolean | optional | Include parent session output history in fork prompt (default: true) |
| `model` | string | optional | Override model for the fork (inherits parent model if omitted) |
| `prompt` | string | optional | New prompt for the forked session (inherits parent prompt if omitted) |
| `provider` | string | optional | Override provider for the fork (inherits parent provider if omitted) |



### `ralphglasses_session_handoff`

Transfer session state to a new session, optionally switching providers. Includes context from observations, cost tracking, and scratchpad.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `source_session_id` | string | **required** | Session ID to transfer from |
| `context_lines` | number | optional | Number of recent observations to include (default: 5) |
| `handoff_reason` | string | optional | Reason for handoff (tracked in handoff record) |
| `include_context` | boolean | optional | Include observation context in handoff (default: true) |
| `stop_source` | boolean | optional | Stop the source session after handoff (default: false) |
| `target_provider` | string | optional | Target provider: claude, gemini, codex (default: same as source) |



### `ralphglasses_session_launch`

Launch a headless LLM CLI session (claude/gemini/codex) for a repo with a `prompt`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | Prompt/task to send |
| `repo` | string | **required** | Repo name |
| `agent` | string | optional | Agent/subagent name. Native for Claude only. |
| `allowed_tools` | string | optional | Comma-separated allowed tools (e.g. Bash,Read,Edit). Supported by Claude and Gemini; unsupported by Codex. |
| `bare` | boolean | optional | Skip hooks/plugins for faster scripted startup |
| `budget_usd` | number | optional | Budget in USD — maximum spend for this session. Native for Claude; externally enforced by ralphglasses for Codex and Gemini. |
| `effort` | string | optional | Thinking effort level: low, medium, high, max |
| `enhance_prompt` | string | optional | Auto-enhance the prompt before launch: local (deterministic), llm (Claude API), auto (try LLM, fallback). Omit to skip enhancement |
| `fallback_model` | string | optional | Auto-fallback model on overload |
| `max_turns` | number | optional | Maximum conversation turns. Native for Claude only. |
| `model` | string | optional | Model to use |
| `no_journal` | string | optional | Skip improvement journal injection: true/false (default: false) |
| `output_schema` | string | optional | JSON schema for structured output validation (Claude: --json-schema, Codex: --output-schema) |
| `provider` | string | optional | LLM provider: codex (default), claude, gemini |
| `session_name` | string | optional | Human-readable session name |
| `system_prompt` | string | optional | Additional system prompt to append. Native for Claude only; use GEMINI.md or AGENTS.md for Gemini/Codex repo instructions. |
| `target_provider` | string | optional | Target LLM provider for prompt enhancement: claude, gemini, openai (defaults to session provider) |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |
| `worktree` | string | optional | Git worktree isolation (true for auto, or branch name). Supported by Claude and Gemini; unsupported for Codex. |



### `ralphglasses_session_list`

List all tracked LLM sessions with status, cost, and turns

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `include_ended` | boolean | optional | Include historical ended sessions from the persistent store when available |
| `provider` | string | optional | Filter by provider: claude, gemini, codex (omit for all) |
| `repo` | string | optional | Filter by repo name (omit for all) |
| `status` | string | optional | Filter by status: running, completed, errored, stopped |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_output`

Get recent output from a session by `id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID |
| `lines` | number | optional | Number of lines to return (default 20, max 100) |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_replay_diff`

Side-by-side comparison of two session JSONL replay files: event alignment, similarity score

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path_a` | string | **required** | Path to first JSONL replay file |
| `path_b` | string | **required** | Path to second JSONL replay file |
| `max_events` | number | optional | Max events in response (default 100) |



### `ralphglasses_session_resume`

Resume a previous LLM CLI session for `repo` using `session_id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `session_id` | string | **required** | Provider session ID to resume (from session status) |
| `prompt` | string | optional | Follow-up prompt (optional) |
| `provider` | string | optional | LLM provider: codex (default), claude, gemini |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_retry`

Re-launch a failed session by `id` with same params, optional overrides

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID to retry |
| `budget_usd` | number | optional | Budget in USD — override budget for retry |
| `model` | string | optional | Override model |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_status`

Get detailed status for a session by `id` including output, cost, and turns

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_stop`

Stop a running session by `id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID to stop |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_stop_all`

Stop all running LLM sessions — emergency cost cutoff

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_session_tail`

Tail session output by `id` with cursor — returns only new lines since last call

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID |
| `cursor` | string | optional | Cursor from previous response (omit for latest) |
| `lines` | number | optional | Max lines to return (default 30, max 100) |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |




## loop

Perpetual development loops: start, status, step, stop, benchmark, baseline, gates, self-test, self-improve


### `ralphglasses_loop_await`

Block until a session or loop completes (replaces sleep anti-pattern)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session or loop ID to wait for |
| `type` | string | **required** | 'session' or 'loop' |
| `poll_interval_seconds` | number | optional | Poll interval in seconds (default 10, min 5) |
| `timeout_seconds` | number | optional | Max wait time in seconds (default 300) |



### `ralphglasses_loop_baseline`

Generate, view, or pin loop performance baseline for a repo

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `action` | string | optional | Action: view (default), refresh, or pin |
| `hours` | number | optional | Window for refresh/pin in hours (default: 48) |



### `ralphglasses_loop_benchmark`

P50/P95 metrics from recent loop observations for a repo

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Look-back window in hours (default: 48) |



### `ralphglasses_loop_gates`

Evaluate regression gates — returns pass/warn/fail report

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `baseline_path` | string | optional | Optional absolute path to a baseline JSON file |
| `hours` | number | optional | Look-back window in hours (default: 24) |



### `ralphglasses_loop_poll`

Non-blocking single status check for a session or loop

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session or loop ID |
| `type` | string | **required** | 'session' or 'loop' |



### `ralphglasses_loop_prune`

Prune stale loop run files by status and age — removes phantom pending/failed loop state that accumulates over time

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `dry_run` | boolean | optional | If true, return count without deleting (default: true) |
| `older_than_hours` | number | optional | Only prune loop runs older than this many hours (default: 72) |
| `repo` | string | optional | Optional repo name filter — only prune runs for this repo |
| `statuses` | string | optional | Comma-separated statuses to prune (default: pending,failed) |



### `ralphglasses_loop_start`

Create a multi-provider planner/worker perpetual development loop for a repo

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `budget_usd` | number | optional | Budget in USD — total budget split 1/3 planner, 2/3 worker |
| `duration_hours` | number | optional | Maximum loop duration in hours (0 = unlimited) |
| `enable_cascade` | boolean | optional | Enable cascade routing (cheap-then-expensive provider) |
| `enable_curriculum` | boolean | optional | Enable curriculum learning (difficulty-based task sorting) |
| `enable_episodic_memory` | boolean | optional | Enable episodic memory (successful trajectory recall) |
| `enable_reflexion` | boolean | optional | Enable reflexion loop (failure correction injection) |
| `enable_uncertainty` | boolean | optional | Enable uncertainty quantification (confidence scoring) |
| `max_concurrent_workers` | number | optional | Maximum concurrent workers (currently only 1 supported) |
| `max_iterations` | number | optional | Maximum loop iterations (0 = unlimited) |
| `planner_model` | string | optional | Planner model (default: gpt-5.4) |
| `planner_provider` | string | optional | Planner provider: claude, gemini, codex (default: codex) |
| `retry_limit` | number | optional | Maximum consecutive failed iterations before step is refused |
| `self_improvement` | boolean | optional | Enable self-improvement mode with autonomous acceptance gate |
| `trace_level` | string | optional | Trace verbosity: none, summary (default), verbose |
| `verifier_model` | string | optional | Verifier model metadata (default: gpt-5.4) |
| `verifier_provider` | string | optional | Verifier provider: claude, gemini, codex (default: codex) |
| `verify_commands` | string | optional | SECURITY: Privileged input. Newline-separated bash commands (default: ./scripts/dev/ci.sh) |
| `worker_model` | string | optional | Worker model (default: gpt-5.4) |
| `worker_provider` | string | optional | Worker provider: claude, gemini, codex (default: codex) |
| `worktree_policy` | string | optional | Worktree isolation policy (default: git) |



### `ralphglasses_loop_status`

Get status for a perpetual development loop by `id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Loop run ID |



### `ralphglasses_loop_step`

Execute one planner/worker/verifier iteration for a loop by `id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Loop run ID |



### `ralphglasses_loop_stop`

Stop a perpetual development loop by `id`

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Loop run ID |



### `ralphglasses_self_improve`

Start a self-improvement loop that autonomously improves a repository — auto-merges safe changes, creates PRs for review-required changes

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `budget_usd` | number | optional | Budget in USD (default: 20.0, split 1/4 planner + 3/4 worker) |
| `duration_hours` | number | optional | Maximum duration in hours (default: 4) |
| `max_iterations` | number | optional | Maximum iterations (default: 5) |
| `trace_level` | string | optional | Trace verbosity: none, summary (default), verbose |



### `ralphglasses_self_test`

Run recursive self-test iterations against a repository using the ralphglasses loop engine

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Absolute path to the repository to test |
| `budget_usd` | number | optional | Budget in USD (default: 5.0) |
| `dry_run` | boolean | optional | Validate config without running iterations (default: false) |
| `iterations` | number | optional | Number of self-test iterations (default: 3) |
| `use_snapshot` | boolean | optional | Restore repo snapshot between iterations (default: true) |




## prompt

Prompt enhancement: analyze, enhance, lint, improve, classify, should_enhance, templates, template_fill


### `ralphglasses_prompt_ab_test`

A/B test two prompts by scoring them across 10 quality dimensions. Returns winner, score diff, confidence, and per-dimension comparison.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt_a` | string | **required** | First prompt to compare |
| `prompt_b` | string | **required** | Second prompt to compare |
| `repo` | string | optional | Repo name for result storage |
| `target_provider` | string | optional | Target provider for scoring: openai (default), claude, gemini |



### `ralphglasses_prompt_analyze`

Score a prompt across 10 quality dimensions (clarity, specificity, structure, examples, etc.) with letter grades and actionable suggestions

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | The prompt text to analyze |
| `target_provider` | string | optional | Target model provider for scoring suggestions: openai (default), claude, gemini |
| `task_type` | string | optional | Override auto-detection: code, troubleshooting, analysis, creative, workflow, general |



### `ralphglasses_prompt_classify`

Classify a prompt's task type (code, troubleshooting, analysis, creative, workflow, general)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | The prompt text to classify |



### `ralphglasses_prompt_enhance`

Run the 13-stage prompt enhancement pipeline (specificity, positive reframing, XML structure, context reorder, format enforcement, etc.)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | The prompt text to enhance |
| `mode` | string | optional | Enhancement mode: local (default, deterministic), llm (Claude/Gemini/OpenAI API), auto (try LLM, fallback to local) |
| `repo` | string | optional | Repo name to load .prompt-improver.yaml config from |
| `target_provider` | string | optional | Target model provider — controls structure style and scoring: openai (default), claude, gemini |
| `task_type` | string | optional | Override auto-detection: code, troubleshooting, analysis, creative, workflow, general |
| `trace_level` | string | optional | Trace verbosity: none, summary (default), verbose |



### `ralphglasses_prompt_improve`

LLM-powered prompt improvement using Claude, Gemini, or OpenAI with domain-specific meta-prompts

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | The prompt text to improve |
| `feedback` | string | optional | Optional feedback to guide the improvement direction |
| `provider` | string | optional | LLM provider for improvement: openai (default, requires OPENAI_API_KEY), claude (requires ANTHROPIC_API_KEY), gemini (requires GOOGLE_API_KEY) |
| `task_type` | string | optional | Override auto-detection: code, troubleshooting, analysis, creative, workflow, general |
| `thinking_enabled` | boolean | optional | Include thinking scaffolding in the improved prompt |



### `ralphglasses_prompt_lint`

Deep-lint a prompt for anti-patterns: unmotivated rules, negative framing, aggressive caps, vague quantifiers, injection risks, cache-unfriendly ordering

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | The prompt text to lint |



### `ralphglasses_prompt_should_enhance`

Check whether a prompt would benefit from enhancement

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | The prompt text to check |
| `repo` | string | optional | Repo name for loading .prompt-improver.yaml config |



### `ralphglasses_prompt_template_fill`

Fill a prompt template by `name` with `vars` (JSON key-value pairs)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Template name |
| `vars` | string | **required** | JSON object of variable key-value pairs |



### `ralphglasses_prompt_templates`

List available prompt templates with descriptions and required variables

*No parameters.*




## fleet

Fleet operations: fleet_status, analytics, submit, budget, workers, dlq, marathon_dashboard, capacity_plan


### `ralphglasses_fleet_analytics`

Cost breakdown by provider/repo/time-period with trend analysis. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `provider` | string | optional | Filter by provider |
| `repo` | string | optional | Filter by repo name |
| `window` | string | optional | Time window as Go duration (e.g. '1h', '24h'). Default: 1h |



### `ralphglasses_fleet_budget`

View or set the fleet-wide budget. Shows spent, remaining, and active work. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `limit` | number | optional | New budget limit in USD (omit to just view current budget) |



### `ralphglasses_fleet_capacity_plan`

Recommend worker count from queue depth and budget. Returns recommended workers, estimated cost, completion time, and cost per worker hour.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `available_budget` | number | **required** | Available budget in USD |
| `queue_depth` | number | **required** | Number of tasks in the queue |
| `avg_task_cost` | number | optional | Average cost per task in USD (auto-estimated from observations if omitted) |
| `avg_task_duration_min` | number | optional | Average task duration in minutes (default: 10) |
| `target_completion_hours` | number | optional | Target completion time in hours (default: 4) |
| `utilization_factor` | number | optional | Worker efficiency factor 0.1-1.0 (default: 0.8 — workers aren't 100% productive) |



### `ralphglasses_fleet_dlq`

Dead letter queue operations for permanently failed fleet work items. Actions: list, retry, purge, depth. Requires fleet coordinator mode.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: list (default), retry, purge, depth |
| `item_id` | string | optional | Work item ID (required for retry action) |



### `ralphglasses_fleet_grafana`

Export a Grafana-compatible JSON dashboard for fleet metrics (session throughput, cost burn rate, provider comparison, error rate, active sessions, budget utilization). Import the output directly into Grafana.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `datasource` | string | optional | Prometheus data source name/UID (default: 'prometheus') |
| `metrics` | string | optional | Comma-separated metric names to include: session_throughput, cost_burn_rate, provider_comparison, error_rate, active_sessions, budget_utilization. Default: all |
| `title` | string | optional | Dashboard title (default: 'Ralphglasses Fleet Metrics') |



### `ralphglasses_fleet_runtime`

Start, stop, restart, inspect, or auto-discover fleet runtime mode parity for the CLI serve command

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: status (default), discover, start, stop, or restart |
| `automation` | boolean | optional | Run repo-local automation supervisors alongside the runtime (default: true) |
| `coordinator_url` | string | optional | Coordinator URL for worker mode |
| `fleet_budget` | number | optional | Fleet-wide budget ceiling in USD (coordinator mode only) |
| `mode` | string | optional | Runtime mode: coordinator or worker (default: worker for start/restart) |
| `port` | number | optional | Fleet port (default: 9473) |



### `ralphglasses_fleet_schedule`

Build a dependency-ordered schedule plan from tasks with dependencies. Uses topological sort to group tasks into parallelizable batches. Returns batch plan, critical path, and depth.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `tasks` | string | **required** | JSON array of task objects: [{"id": "build", "name": "Build", "dependencies": ["lint"], "priority": 5}, ...] |



### `ralphglasses_fleet_status`

Fleet-wide dashboard: aggregate status, costs, health, and alerts across all repos and sessions in one call

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `limit` | number | optional | Max repos to return in full mode (default 50) |
| `offset` | number | optional | Pagination offset for repos (default 0) |
| `repo` | string | optional | Filter to a specific repo name |
| `summary_only` | boolean | optional | Return compact JSON with just repo names, session counts, and total spend instead of full dump |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_fleet_submit`

Submit work for `repo` with `prompt` to the distributed fleet queue. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | Task prompt |
| `repo` | string | **required** | Repo name |
| `budget_usd` | number | optional | Budget in USD (default: 5.0) |
| `priority` | number | optional | Priority 0-10 (default: 5, higher = first) |
| `provider` | string | optional | codex (default), claude, gemini |



### `ralphglasses_fleet_workers`

List registered fleet workers with status, capacity, and spend. Optionally pause, resume, or drain a worker. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Worker action: pause, resume, or drain (omit to list) |
| `worker_id` | string | optional | Worker ID (required for pause/resume/drain actions) |



### `ralphglasses_marathon`

Start, resume, inspect, or stop the CLI marathon workflow

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: status (default), start, resume, or stop |
| `budget_usd` | number | optional | Maximum budget in USD (default: 10.0) |
| `checkpoint_interval` | string | optional | Checkpoint interval (default: 10m) |
| `duration` | string | optional | Maximum duration (default: 1h) |
| `repo` | string | optional | Target repo name |



### `ralphglasses_marathon_dashboard`

Compact marathon status: burn rate, stale sessions, team progress, alerts

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `stale_threshold_min` | number | optional | Minutes idle before flagged stale (default 5) |




## repo

Repo management: health, optimize, scaffold, claudemd_check, snapshot


### `ralphglasses_claudemd_check`

Health-check a repo's CLAUDE.md for common issues (length, inline code, overtrigger language, missing headers)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |



### `ralphglasses_repo_health`

Composite health check: circuit breaker, budget, staleness, errors, active sessions

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |



### `ralphglasses_repo_optimize`

Analyze and optimize ralph config files — detect misconfigs, missing settings, stale plans

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path` | string | **required** | Repo root path |
| `dry_run` | string | optional | Report only, don't modify: true/false (default: true) |
| `focus` | string | optional | Focus area: config, prompt, plan, all (default: all) |



### `ralphglasses_repo_scaffold`

Create/initialize ralph config files (.ralph/, .ralphrc, PROMPT.md, AGENT.md, fix_plan.md) for a repo

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path` | string | **required** | Repo root path |
| `force` | string | optional | Overwrite existing files: true/false (default: false) |
| `minimal` | boolean | optional | Generate the minimal .ralphrc variant |
| `project_name` | string | optional | Project name override (defaults to directory name) |
| `project_type` | string | optional | Project type override (auto-detected from go.mod, package.json, etc.) |



### `ralphglasses_repo_surface_audit`

Audit repo instruction/config surfaces used by Codex, Claude, Gemini, and MCP clients

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |



### `ralphglasses_snapshot`

Save or list fleet state snapshots

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: save (default) or list |
| `name` | string | optional | Snapshot name (auto-generated if omitted) |
| `repo` | string | optional | Target repo name for snapshot storage (resolved from CWD if omitted) |



### `ralphglasses_worktree_cleanup`

Clean up stale loop worktrees older than a given age — skips locked/active worktrees

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `dry_run` | boolean | optional | Preview stale worktrees without deleting them |
| `max_age_hours` | number | optional | Max age in hours before cleanup (default 24) |



### `ralphglasses_worktree_create`

Create a new git worktree for a repo under .ralph/worktrees/manual/

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Worktree name (sanitized for filesystem) |
| `repo` | string | **required** | Repo name |



### `ralphglasses_worktree_list`

List git worktrees for a repo with optional dirty/stale filtering

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `dirty_only` | boolean | optional | Only include dirty worktrees |
| `include_stale` | boolean | optional | Annotate stale worktrees using stale_after_hours |
| `stale_after_hours` | number | optional | Age threshold in hours for stale detection (default: 24) |




## roadmap

Roadmap automation: parse, analyze, research, expand, export


### `ralphglasses_roadmap_analyze`

Compare roadmap vs codebase at `path` — find gaps, stale checkboxes, ready tasks, orphaned code

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path` | string | **required** | Repo root path |
| `category` | string | optional | Filter by category: gaps, stale, orphaned, ready |
| `file` | string | optional | Override filename (default: ROADMAP.md) |
| `limit` | number | optional | Max items per category (default 20) |



### `ralphglasses_roadmap_expand`

Generate proposed roadmap expansions at `path` from analysis gaps and research findings

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path` | string | **required** | Repo root path |
| `file` | string | optional | Override filename (default: ROADMAP.md) |
| `limit` | number | optional | Max proposals to return (default 20) |
| `phase` | string | optional | Filter proposals to a specific phase |
| `research` | string | optional | Research topics to include (runs research internally) |
| `style` | string | optional | Expansion style: conservative, balanced, aggressive (default: balanced) |
| `summary_only` | boolean | optional | Drop proposal markdown and return truncated summaries only |



### `ralphglasses_roadmap_export`

Export roadmap items at `path` as structured task specs for ralph loop consumption

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path` | string | **required** | Repo root path |
| `file` | string | optional | Override filename (default: ROADMAP.md) |
| `format` | string | optional | Output format: rdcycle, fix_plan, progress, launch_ready (default: rdcycle). launch_ready enriches tasks with difficulty_score, suggested_provider, estimated_budget_usd |
| `max_tasks` | number | optional | Max tasks to export (default 20) |
| `phase` | string | optional | Filter by phase name (default: all) |
| `respect_deps` | string | optional | Skip tasks with unmet deps (default: true) |
| `section` | string | optional | Filter by section name (default: all) |
| `status` | string | optional | Filter by status: incomplete (default), complete, all |



### `ralphglasses_roadmap_parse`

Parse ROADMAP.md at `path` into structured JSON (phases, sections, tasks, deps, completion stats)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path` | string | **required** | Repo root or direct .md path |
| `file` | string | optional | Override filename (default: ROADMAP.md) |
| `max_depth` | number | optional | Depth of detail: 0=phases only, 1=phases+sections, 2=full (default 2) |
| `phase` | string | optional | Filter to a specific phase name |
| `summary_only` | boolean | optional | Return compact summary (phase counts, completion %) instead of full task details |



### `ralphglasses_roadmap_prioritize`

Score and rank uncompleted roadmap items by impact, effort, and dependency readiness. Detects section-level blocking and adds phase momentum bonus. Returns prioritized list and recommended next sprint.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `phase_filter` | string | optional | Filter to phases containing this string (e.g., 'Phase 10') |
| `top_n` | number | optional | Number of items to return (default: 20) |
| `weights` | string | optional | JSON weights: {"impact": 0.4, "effort": 0.3, "dependency": 0.3} |



### `ralphglasses_roadmap_research`

Search GitHub for relevant repos and tools at `path` that unlock new capabilities

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `path` | string | **required** | Repo root path |
| `limit` | number | optional | Max results (default 10) |
| `topics` | string | optional | Search topics (inferred from go.mod/README if omitted) |




## team

Agent teams and definitions: team_create, team_status, team_delegate, agent_define, agent_list, agent_compose


### `ralphglasses_agent_compose`

Create a composite agent by layering multiple existing agent definitions

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `agents` | string | **required** | Comma-separated agent names to compose |
| `name` | string | **required** | Name for the composite agent |
| `repo` | string | **required** | Repo name |
| `model` | string | optional | Override model for composite agent |
| `provider` | string | optional | Provider: codex (default), claude, gemini |



### `ralphglasses_agent_define`

Create or update an agent definition for a repo (supports all providers)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Agent name |
| `prompt` | string | **required** | Agent system prompt / instructions (markdown) |
| `repo` | string | **required** | Repo name |
| `description` | string | optional | Agent description |
| `max_turns` | number | optional | Max turns for this agent |
| `model` | string | optional | Model override (sonnet, opus, haiku) |
| `provider` | string | optional | Target provider: codex (default, .codex/agents/*.toml), claude (.claude/agents/), gemini (.gemini/commands/*.toml) |
| `tools` | string | optional | Comma-separated allowed tools |



### `ralphglasses_agent_list`

List available agent definitions for a repo (supports all providers)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `provider` | string | optional | Filter by provider: codex (default), claude, gemini, or 'all' |



### `ralphglasses_team_create`

Create an agent team by `name` for `repo`; Codex teams use the structured autonomous runtime with planner/worker orchestration

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Team name |
| `repo` | string | **required** | Repo name |
| `tasks` | string | **required** | Newline-separated task descriptions |
| `a2a_agent_url` | string | optional | Reserved for future A2A support; ignored today |
| `autostart` | boolean | optional | Start the background team controller immediately; defaults true for Codex teams |
| `budget_usd` | number | optional | Budget in USD — total budget for the team |
| `dry_run` | boolean | optional | Return team configuration without launching sessions |
| `execution_backend` | string | optional | Worker execution backend: local or fleet |
| `lead_agent` | string | optional | Provider-native agent definition name for the lead session. Not supported for Codex teams. |
| `max_concurrency` | number | optional | Maximum concurrent worker tasks (default 2) |
| `max_retries` | number | optional | Maximum retries per task (default 2) |
| `model` | string | optional | Model for lead session |
| `provider` | string | optional | LLM provider for lead: codex (default), claude, gemini |
| `target_branch` | string | optional | Repo branch to reconcile/promote into (default main) |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |
| `worker_model` | string | optional | Model override for worker sessions |
| `worker_provider` | string | optional | Default LLM provider for worker tasks: codex (default), claude, gemini |
| `worktree_policy` | string | optional | Worker isolation mode: per_worker (default for Codex) or shared |



### `ralphglasses_team_delegate`

Add a new task to an existing team

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Team name |
| `task` | string | **required** | Task description to delegate |
| `provider` | string | optional | LLM provider override for this task: claude, gemini, codex |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |



### `ralphglasses_team_status`

Get team status including lead session, tasks, and progress

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Team name |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |




## tenant

Workspace tenant administration: list, create, status, rotate trigger token, and batch role leaderboards


### `ralphglasses_tenant_create`

Create or update a workspace tenant

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `tenant_id` | string | **required** | Tenant ID |
| `allowed_repo_roots` | string | optional | Comma-separated allowed repo roots |
| `budget_cap_usd` | number | optional | Budget cap in USD |
| `display_name` | string | optional | Display name |



### `ralphglasses_tenant_list`

List all configured workspace tenants

*No parameters.*



### `ralphglasses_tenant_role_leaderboards`

Generate top-role leaderboards for one tenant or all tenants in a batch

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `include_ended` | boolean | optional | Include persisted ended sessions (default: true) |
| `limit` | number | optional | Maximum role entries per tenant (default: 10) |
| `tenant_id` | string | optional | Optional tenant ID; omit to return all tenants |



### `ralphglasses_tenant_rotate_trigger_token`

Rotate the bearer token used by the trigger HTTP API for one tenant

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `tenant_id` | string | **required** | Tenant ID |



### `ralphglasses_tenant_status`

Get tenant details plus current active session/team counts

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `tenant_id` | string | **required** | Tenant ID |




## awesome

Awesome-list research: fetch, analyze, diff, report, sync


### `ralphglasses_awesome_analyze`

Deep-analyze repos: fetch READMEs, score value/complexity vs ralph capabilities

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `max_workers` | number | optional | Concurrent README fetches (default 5) |
| `repo` | string | optional | GitHub repo (default: hesreallyhim/awesome-claude-code) |



### `ralphglasses_awesome_diff`

Compare current awesome-list against previous fetch (new/removed entries)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `save_to` | string | **required** | Repo path where previous index is saved |
| `repo` | string | optional | GitHub repo (default: hesreallyhim/awesome-claude-code) |



### `ralphglasses_awesome_fetch`

Fetch and parse an awesome-list README into structured entries with categories

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | optional | GitHub repo (default: hesreallyhim/awesome-claude-code) |



### `ralphglasses_awesome_report`

Generate formatted report from saved analysis results

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `save_to` | string | **required** | Repo path where analysis is saved |
| `format` | string | optional | Output format: json or markdown (default: markdown) |



### `ralphglasses_awesome_sync`

Full pipeline: fetch awesome-list -> diff -> analyze new entries -> report -> save

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `save_to` | string | **required** | Repo path to save results |
| `full_rescan` | string | optional | Re-analyze all entries, not just new: true/false (default: false) |
| `max_workers` | number | optional | Concurrent README fetches (default 5) |
| `repo` | string | optional | GitHub repo (default: hesreallyhim/awesome-claude-code) |




## advanced

Advanced: journals, tool benchmark, circuit breaker reset


### `ralphglasses_circuit_reset`

Reset circuit breaker state for a named service, re-enabling requests after failures. Use 'enhancer' for the LLM prompt enhancer circuit breaker, or a repo name for its file-based circuit breaker.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `service` | string | **required** | Service/circuit to reset: 'enhancer' or a repo name |



### `ralphglasses_journal_prune`

Compact improvement journal to prevent unbounded growth

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `dry_run` | string | optional | Preview only, don't modify: true/false (default: true) |
| `keep` | number | optional | Number of entries to keep (default 100) |



### `ralphglasses_journal_read`

Read improvement journal entries for a repo with synthesized context

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `limit` | number | optional | Max entries to return (default 10) |



### `ralphglasses_journal_write`

Manually write an improvement note to a repo's journal

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `failed` | string | optional | Comma-separated items that failed |
| `session_id` | string | optional | Associated session ID (optional) |
| `suggest` | string | optional | Comma-separated suggestions |
| `worked` | string | optional | Comma-separated items that worked |



### `ralphglasses_tool_benchmark`

Per-tool performance benchmarks: latency percentiles, success rates, and regression detection

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `compare` | string | optional | Include regression analysis vs previous baseline: true/false (default: false) |
| `hours` | number | optional | Time window in hours (default 24) |
| `tool` | string | optional | Filter to a specific tool name |




## events

Fleet event bus: query and poll for session, cost, loop, and circuit events


### `ralphglasses_event_list`

Query recent fleet events from the event bus

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `limit` | number | optional | Max events to return (default 50) |
| `offset` | number | optional | Skip first N results for pagination (default 0) |
| `provider` | string | optional | Filter by provider |
| `repo` | string | optional | Filter by repo name |
| `session_id` | string | optional | Filter by session ID |
| `since` | string | optional | ISO timestamp filter |
| `type` | string | optional | Filter by event type (e.g. session.started, cost.update) |
| `types` | string | optional | Comma-separated event types to filter (OR). Alternative to single 'type' param |
| `until` | string | optional | ISO timestamp upper bound (exclusive) |



### `ralphglasses_event_poll`

Poll for new fleet events since last check. Cursor-based for efficient mobile polling.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `cursor` | string | optional | Cursor from previous response (omit for recent) |
| `limit` | number | optional | Max events (default 20, max 50) |
| `type` | string | optional | Filter by event type (e.g. session.started, cost.update) |




## feedback

Provider feedback: performance profiles, recommendations, bandit stats, confidence calibration


### `ralphglasses_bandit_status`

View multi-armed bandit arm statistics for provider selection

*No parameters.*



### `ralphglasses_confidence_calibration`

View calibrated confidence model weights, training status, and feature importances

*No parameters.*



### `ralphglasses_feedback_profiles`

View feedback profiles: per-task-type and per-provider performance data from journal analysis. Auto-seeds from observations when empty.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: 'get' (default) returns profiles, 'seed' forces re-seed from observations |
| `type` | string | optional | Filter: prompt, provider, or omit for both |



### `ralphglasses_provider_capabilities`

Inspect the provider capability matrix for Claude, Codex, and Gemini, including native, emulated, and install-dependent features

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `provider` | string | optional | Optional provider filter: claude, codex, gemini |



### `ralphglasses_provider_compare`

Compare Claude, Codex, and Gemini repo/runtime surfaces and optionally include task-specific recommendation data

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `task` | string | optional | Optional task description to include recommendation and feedback context |



### `ralphglasses_provider_recommend`

Recommend best provider + model + budget for a task based on feedback profiles and cost normalization

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `task` | string | **required** | Task description (e.g. 'fix lint errors', 'add search feature') |




## eval

Offline evaluation: counterfactual analysis, Bayesian A/B testing, frequentist significance testing, changepoint detection


### `ralphglasses_anomaly_detect`

Detect anomalies in metric streams using sliding-window z-score analysis

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `metric` | string | **required** | Metric to analyze — full names (total_cost_usd, total_latency_ms, confidence, difficulty_score) or shorthands (cost, latency, difficulty) |
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Observation window in hours (default: 168) |



### `ralphglasses_eval_ab_test`

Bayesian A/B test comparing providers or time periods using Beta-Bernoulli model

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `mode` | string | **required** | Comparison mode: providers or periods |
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Observation window in hours (default: 168) |
| `provider_a` | string | optional | First provider (providers mode) |
| `provider_b` | string | optional | Second provider (providers mode) |
| `split_hours_ago` | number | optional | Hours ago to split time periods (periods mode) |



### `ralphglasses_eval_changepoints`

Detect performance shifts using CUSUM changepoint detection on loop observations

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Observation window in hours (default: 168) |
| `metric` | string | optional | Specific metric to analyze (completion_rate, cost, latency, confidence, difficulty). Omit for all. |



### `ralphglasses_eval_counterfactual`

Estimate outcomes under hypothetical policy changes using inverse propensity scoring on loop observations

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `policy` | string | **required** | Policy type: cascade_threshold or provider_routing |
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Observation window in hours (default: 168 = 7 days) |
| `provider` | string | optional | Target provider for provider_routing policy |
| `task_type` | string | optional | Task type for provider_routing policy |
| `threshold` | number | optional | New cascade threshold for cascade_threshold policy (default: 0.6) |



### `ralphglasses_eval_define`

Validate and parse an A/B test definition from YAML content. Returns the parsed definition or validation errors.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `yaml_content` | string | **required** | YAML content defining the A/B test (name, variants, metrics, sample_size, timeout) |



### `ralphglasses_eval_significance`

Frequentist significance testing (z-test/t-test) with combined Bayesian comparison report

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `mode` | string | **required** | Comparison mode: providers, periods, or cost |
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Observation window in hours (default: 168) |
| `provider_a` | string | optional | First provider (providers/cost mode) |
| `provider_b` | string | optional | Second provider (providers/cost mode) |
| `split_hours_ago` | number | optional | Hours ago to split time periods (periods mode) |



### `ralphglasses_provider_benchmark`

Compare providers using a standardized task suite (code gen, explanation, debugging, refactoring, test writing). Returns quality scores, cost estimates, and winner recommendation.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `iterations` | number | optional | Number of iterations per task (default: 3, max: 10) |
| `providers` | string | optional | Comma-separated providers to benchmark (default: codex,gemini,claude) |
| `repo` | string | optional | Repo for result storage |




## fleet_h

Fleet intelligence: blackboard coordination, A2A task delegation, cost forecasting, cost recommendations


### `ralphglasses_a2a_offers`

List open agent-to-agent task delegation offers. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

*No parameters.*



### `ralphglasses_blackboard_put`

Write an entry to the blackboard for fleet coordination. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `key` | string | **required** | Entry key |
| `namespace` | string | **required** | Entry namespace |
| `value` | string | **required** | JSON object value |
| `ttl_seconds` | number | optional | Time-to-live in seconds (0 for no expiry) |
| `writer_id` | string | optional | Writer identifier |



### `ralphglasses_blackboard_query`

Query blackboard entries by namespace for fleet worker coordination. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `namespace` | string | **required** | Namespace to query |



### `ralphglasses_cost_forecast`

Cost burn rate, anomaly detection, and budget exhaustion ETA. Requires a fleet coordinator started via `ralphglasses serve --coordinator` and an MCP client configured with `RALPH_FLEET_URL`.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `budget_remaining` | number | optional | Remaining budget in USD for exhaustion ETA (default: 0) |



### `ralphglasses_cost_recommend`

Analyze cost history and recommend config changes: provider switches, budget pacing, anomaly responses, cache optimization, model downgrades

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `budget_hours` | number | optional | Desired budget runway in hours (default: 8) |
| `budget_remaining` | number | optional | Remaining budget in USD for pacing recommendations |
| `concurrency` | number | optional | Current session concurrency for pacing recommendations |
| `min_samples` | number | optional | Minimum samples per provider for comparison (default: 5) |
| `type` | string | optional | Filter by recommendation type: provider_switch, budget_pacing, anomaly_response, cache_optimize, model_downgrade |




## observability

Observation queries, scratchpad automation, loop wait/poll, coverage, cost estimation, merge verification


### `ralphglasses_cost_estimate`

Pre-launch cost estimate for a session or loop

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `provider` | string | **required** | Provider: claude, gemini, or codex |
| `iterations` | number | optional | Loop iterations for mode=loop (default 3) |
| `mode` | string | optional | 'session' or 'loop' (default session) |
| `model` | string | optional | Model name (uses provider default if omitted) |
| `output_tokens_per_turn` | number | optional | Output tokens per turn (default 2000) |
| `prompt_tokens` | number | optional | Prompt length in tokens (default 5000) |
| `repo` | string | optional | Repo name for historical calibration |
| `turns` | number | optional | Expected conversation turns (default 5) |



### `ralphglasses_coverage_report`

Run go test -coverprofile and report per-package coverage vs threshold

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name or absolute path |
| `packages` | string | optional | Package pattern (default ./...) |
| `threshold` | number | optional | Coverage threshold percentage (default 70) |



### `ralphglasses_merge_verify`

Run build+vet+test sequence to verify a merge

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo path (absolute) |
| `coverage` | boolean | optional | Include coverage profile |
| `fast` | boolean | optional | Use -short flag for faster tests |
| `packages` | string | optional | Package pattern to test (default ./...) |
| `race` | boolean | optional | Enable -race detector (default true) |



### `ralphglasses_observation_correlate`

Link observations to git commits by timestamp proximity

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `hours` | number | optional | Hours of history to correlate (default 24) |



### `ralphglasses_observation_query`

Filter and paginate loop observations from .ralph/logs/loop_observations.jsonl

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Hours of history to query (default 48) |
| `limit` | number | optional | Max results (default 50, max 500) |
| `loop_id` | string | optional | Filter by loop ID |
| `provider` | string | optional | Filter by provider |
| `since` | string | optional | RFC3339 timestamp lower bound (overrides hours if set) |
| `status` | string | optional | Filter by status (pass, fail, error) |
| `until` | string | optional | RFC3339 timestamp upper bound |



### `ralphglasses_observation_summary`

Aggregate observation stats with p50/p95/p99 percentiles via SummarizeObservations

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `hours` | number | optional | Hours of history (default 48) |
| `loop_id` | string | optional | Filter by loop ID |
| `since` | string | optional | RFC3339 timestamp lower bound (overrides hours if set) |
| `until` | string | optional | RFC3339 timestamp upper bound |



### `ralphglasses_scratchpad_append`

Append a markdown note to a scratchpad file

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `content` | string | **required** | Markdown content to append |
| `name` | string | **required** | Scratchpad name |
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |
| `section` | string | optional | Optional section header to add before content |



### `ralphglasses_scratchpad_context`

Auto-enrich scratchpad with current system context: fleet status, observations, routing, autonomy level

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Scratchpad name |
| `sections` | string | **required** | Comma-separated sections: fleet, observations, routing, autonomy, all |
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |



### `ralphglasses_scratchpad_delete`

Delete a numbered finding from a scratchpad file

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `finding_id` | string | **required** | Finding number to delete (e.g. '2') |
| `scratchpad` | string | **required** | Scratchpad name |
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |



### `ralphglasses_scratchpad_list`

List all scratchpad files in .ralph/

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |



### `ralphglasses_scratchpad_read`

Read a .ralph/{name}_scratchpad.md file

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Scratchpad name (e.g. 'tool_improvement') |
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |



### `ralphglasses_scratchpad_reason`

Record intermediate reasoning between tool calls: dimension-to-stage mapping, rate cards, prune thresholds, provider selection

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Scratchpad name |
| `topic` | string | **required** | Reasoning topic: enhance_stages, rate_cards, prune_thresholds, provider_selection |
| `input` | string | optional | JSON string with context data for the reasoning |
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |



### `ralphglasses_scratchpad_resolve`

Mark a numbered scratchpad item as resolved

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `item_number` | number | **required** | Item number to resolve |
| `name` | string | **required** | Scratchpad name |
| `resolution` | string | **required** | Resolution description |
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |



### `ralphglasses_scratchpad_validate`

Validate scratchpad entries against expected constraints: score inflation, path errors, budget mismatches, no-op iterations

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `check` | string | **required** | Validation type: scores, paths, budget, noops, all |
| `name` | string | **required** | Scratchpad name |
| `repo` | string | optional | Repo name (auto-detected from CWD; required when multiple repos are scanned) |




## rdcycle

R&D cycle automation — finding-to-task conversion, cycle planning, baselines, merging, and scheduling


### `ralphglasses_budget_forecast`

Predict cost of N more iterations based on historical cost observations

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `loop_id` | string | **required** | Loop run ID to forecast |
| `iterations` | number | optional | Number of iterations to forecast (default 10) |
| `repo` | string | optional | Repo name (auto-detected if omitted) |



### `ralphglasses_cycle_advance`

Advance active cycle to next phase in state machine

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `cycle_id` | string | optional | Cycle ID (default: active cycle) |



### `ralphglasses_cycle_baseline`

Snapshot current repo metrics as a cycle baseline

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `metrics` | string | optional | Comma-separated metric names to capture (default: all) |



### `ralphglasses_cycle_create`

Create a new R&D cycle with objective and success criteria

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `objective` | string | **required** | Cycle objective |
| `repo` | string | **required** | Repository name or path |
| `criteria` | string | optional | Comma-separated success criteria |
| `name` | string | optional | Cycle name (default: 'cycle') |



### `ralphglasses_cycle_fail`

Mark a cycle as failed with error message

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `cycle_id` | string | optional | Cycle ID (default: active cycle) |
| `error` | string | optional | Error message |



### `ralphglasses_cycle_list`

List all R&D cycles for a repo, sorted by most recent

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `limit` | number | optional | Max results (default 20) |



### `ralphglasses_cycle_merge`

Merge parallel worktree results with conflict detection

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `worktree_paths` | string | **required** | Comma-separated worktree paths to merge |
| `conflict_strategy` | string | optional | Conflict resolution: ours, theirs, or manual (default: manual) |



### `ralphglasses_cycle_plan`

Generate a prioritized task plan for the next R&D cycle from unresolved findings

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `budget` | number | optional | Budget in USD (default: 5.0) |
| `max_tasks` | number | optional | Maximum tasks to include (default: 10) |
| `previous_cycle_id` | string | optional | Previous cycle ID for continuity |



### `ralphglasses_cycle_run`

Drive a full R&D cycle synchronously through all phases: create, baseline, plan, execute, observe, synthesize, complete

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `objective` | string | **required** | Cycle objective |
| `repo` | string | **required** | Repository name or path |
| `criteria` | string | optional | Comma-separated success criteria |
| `max_tasks` | number | optional | Max tasks to plan (default 10) |
| `name` | string | optional | Cycle name (default: 'cycle') |
| `repo_path` | string | optional | Deprecated alias for repo; retained for backward compatibility |



### `ralphglasses_cycle_schedule`

Schedule recurring R&D cycles with cron expressions

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `cron_expr` | string | **required** | Cron expression (e.g., '0 2 * * 1' for Monday 2am) |
| `repo` | string | **required** | Repository name or path |
| `criteria` | string | optional | Comma-separated success criteria when cycle_config is omitted |
| `cycle_config` | string | optional | JSON cycle configuration |
| `enabled` | boolean | optional | Whether the schedule is active (default: true) |
| `job_kind` | string | optional | Scheduled job kind (default: cycle) |
| `max_tasks` | number | optional | Max cycle tasks when cycle_config is omitted |
| `name` | string | optional | Cycle name for cycle schedules |
| `objective` | string | optional | Cycle objective when cycle_config is omitted |



### `ralphglasses_cycle_status`

Get current state of a cycle including tasks, findings, and synthesis

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `cycle_id` | string | optional | Cycle ID (default: active cycle) |



### `ralphglasses_cycle_synthesize`

Set cycle synthesis and advance to complete

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `summary` | string | **required** | Synthesis summary |
| `accomplished` | string | optional | Comma-separated list of accomplishments |
| `cycle_id` | string | optional | Cycle ID (default: active cycle) |
| `next_objective` | string | optional | Objective for next cycle |
| `patterns` | string | optional | Comma-separated patterns observed |
| `remaining` | string | optional | Comma-separated list of remaining work |



### `ralphglasses_diff_review`

Auto-review a git diff for quality issues: scope creep, missing tests, TODOs, style

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name or path |
| `checks` | string | optional | CSV of checks: scope_creep, missing_tests, style, todos (default: all) |
| `ref` | string | optional | Git ref to review (default HEAD) |



### `ralphglasses_finding_reason`

Analyze scratchpad findings for root causes by category clustering

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Scratchpad name (e.g., 'tool_improvement') |
| `repo` | string | optional | Repo name (auto-detected if omitted) |



### `ralphglasses_finding_to_task`

Convert a scratchpad finding into a structured loop task spec

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `finding_id` | string | **required** | Finding ID (e.g., FINDING-240) |
| `repo` | string | **required** | Repository name or path |
| `scratchpad_name` | string | **required** | Scratchpad filename without path |



### `ralphglasses_loop_replay`

Replay a failed loop iteration with modified parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `iteration` | number | **required** | Iteration number to replay |
| `loop_id` | string | **required** | Loop run ID |
| `overrides` | string | optional | JSON overrides: {model, provider, budget} |
| `repo` | string | optional | Repo name (auto-detected if omitted) |




## plugin

Plugin management: list, info, enable, disable registered plugins


### `ralphglasses_plugin_disable`

Disable an active plugin

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Plugin name to disable |



### `ralphglasses_plugin_enable`

Enable a disabled plugin

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Plugin name to enable |



### `ralphglasses_plugin_info`

Show detailed information for a specific plugin

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Plugin name |



### `ralphglasses_plugin_list`

List all registered plugins with name, version, status, and type (builtin/yaml/grpc)

*No parameters.*




## sweep

Cross-repo audit sweeps: generate optimized prompts, fan-out sessions, monitor, nudge stalled sessions, schedule recurring checks


### `ralphglasses_sweep_generate`

Generate an optimized audit prompt using the 13-stage enhancer pipeline. Returns enhanced prompt text, quality score, and stages applied.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `custom_prompt` | string | optional | Custom base prompt instead of built-in template. Will be enhanced through the pipeline. |
| `target_provider` | string | optional | Target model provider for structure style: openai (default), claude, gemini |
| `task_type` | string | optional | Task type for prompt template: audit (default), fix, review, improve |



### `ralphglasses_sweep_launch`

Launch an enhanced prompt against multiple repos as parallel sessions. Returns a sweep_id for tracking all sessions as a group.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | The prompt to run (use sweep_generate output or provide your own). Use REPO_PLACEHOLDER for repo name substitution. |
| `allowed_tools` | string | optional | Comma-separated allowed tools (default: read-only tools for plan mode) |
| `budget_usd` | number | optional | Per-session budget in USD (default: 5.0) |
| `effort` | string | optional | Effort level: low, medium, high, max |
| `enhance_prompt` | string | optional | Enhance each repo's prompt before launch: local (default), llm, auto, none |
| `limit` | number | optional | Max repos to launch against (default 10) |
| `max_sweep_budget_usd` | number | optional | Total sweep budget cap in USD (default 100). Rejects launch if estimated cost exceeds this. |
| `max_turns` | number | optional | Max turns per session (default 50). Prevents runaway sessions. |
| `model` | string | optional | Model to use: opus (default), sonnet, haiku |
| `permission_mode` | string | optional | Claude permission mode: plan (default, read-only), auto, default |
| `repos` | string | optional | JSON array of repo names, or "active" for recently-active repos, or "all". Default: active |
| `session_persistence` | boolean | optional | Persist session history to disk (default false for sweeps). |
| `sweep_concurrency` | number | optional | Max simultaneous session launches (default 10). Use 1 for serial, higher for large idle machines. |



### `ralphglasses_sweep_nudge`

Detect and restart stalled sessions in a sweep. Identifies sessions idle beyond threshold and restarts them with the same prompt.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sweep_id` | string | **required** | Sweep ID to nudge |
| `action` | string | optional | Action for stalled sessions: restart (default), skip |
| `stale_threshold_min` | number | optional | Minutes idle before a session is considered stalled (default 5) |



### `ralphglasses_sweep_push`

Push all repos in a sweep that have unpushed commits to their remote.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sweep_id` | string | **required** | Sweep ID whose repos to push |
| `dry_run` | boolean | optional | Show what would be pushed without pushing (default false) |



### `ralphglasses_sweep_report`

Generate a Markdown or JSON summary of a completed sweep: per-repo status, commits, costs, and changes.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sweep_id` | string | **required** | Sweep ID to report on |
| `format` | string | optional | Output format: markdown (default), json |



### `ralphglasses_sweep_retry`

Re-launch only failed or errored sessions from a sweep, preserving all original settings.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sweep_id` | string | **required** | Sweep ID to retry |
| `budget_usd` | number | optional | Override per-session budget for retry (default: use original) |



### `ralphglasses_sweep_schedule`

Set up recurring status checks for a sweep at configurable intervals. Optionally auto-nudges stalled sessions. Returns a task_id for cancellation.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sweep_id` | string | **required** | Sweep ID to monitor |
| `auto_nudge` | boolean | optional | Automatically restart stalled sessions (default false) |
| `interval_minutes` | number | optional | Check interval in minutes (default 5) |
| `max_checks` | number | optional | Stop after N checks, 0 for unlimited (default 0) |
| `max_sweep_budget_usd` | number | optional | Abort sweep and stop all sessions if total spend exceeds this (default 0 = no cap) |



### `ralphglasses_sweep_status`

Dashboard for a sweep: per-repo status, total cost, completion percentage, stalled sessions, and optional output tails.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `sweep_id` | string | **required** | Sweep ID returned by sweep_launch |
| `verbose` | boolean | optional | Include last output lines per session (default false) |




## rc

Remote control — send prompts, read output, and act on sessions from mobile or scripted contexts


### `ralphglasses_dispatch`

Unified cross-provider dispatch for mobile RC. Sends prompts, stops, pauses, resumes, or retries sessions with auto provider selection.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | **required** | Action: send, stop, pause, resume, retry |
| `repo` | string | **required** | Repo name |
| `prompt` | string | optional | Prompt text (required for send action) |
| `provider` | string | optional | Provider: claude, codex, gemini, or auto (default: auto) |



### `ralphglasses_fleet_summary`

One-call fleet overview: total sessions, active/stopped counts, total cost, per-repo status. Compact text for mobile.

*No parameters.*



### `ralphglasses_rc_act`

Quick fleet action: stop, stop_all, pause, resume, retry. Single tool for all control actions.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | **required** | Action: stop, stop_all, pause, resume, retry |
| `target` | string | optional | Session ID or repo name (required except stop_all) |



### `ralphglasses_rc_read`

Read recent output from most active session. Combines tail + status for mobile.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `cursor` | string | optional | Cursor from previous call -- only new output |
| `id` | string | optional | Session ID (omit for most recently active) |
| `lines` | number | optional | Max lines (default 10, max 30) |



### `ralphglasses_rc_send`

Send prompt to repo -- auto-stops existing session, launches new. The 'input' tool for remote control.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | What to tell the agent |
| `repo` | string | **required** | Repo name |
| `budget_usd` | number | optional | Budget in USD (default: 5.0) |
| `model` | string | optional | Override model |
| `provider` | string | optional | codex (default), claude, gemini |
| `resume` | string | optional | true to resume last session instead of fresh start |



### `ralphglasses_rc_status`

Compact fleet overview for mobile: active sessions, costs, alerts in readable text

*No parameters.*




## autonomy

Autonomy management — view/set autonomy level, inspect supervisor status, review and override autonomous decisions, track HITL events


### `ralphglasses_automation_policy`

View, set, or recommend repo-local subscription-window automation policy for Codex pause/resume pacing.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `action` | string | optional | Action: get, set, recommend (default: get) |
| `default_model` | string | optional | Default model for queued work |
| `default_task_budget_usd` | number | optional | Default budget for queued work items |
| `default_task_max_turns` | number | optional | Default max turns for queued work items |
| `enabled` | boolean | optional | Enable or disable subscription-window automation |
| `max_concurrent_sessions` | number | optional | Max concurrent automated sessions (v1 only supports 1) |
| `provider` | string | optional | Provider to automate (default: codex) |
| `reset_anchor` | string | optional | RFC3339 anchor timestamp when using fixed reset_window_hours |
| `reset_cron` | string | optional | 5-field cron for subscription reset timing |
| `reset_window_hours` | number | optional | Window size in hours when using reset_anchor |
| `resume_backoff_minutes` | number | optional | Backoff between failed auto-resume attempts |
| `target_utilization_pct` | number | optional | Target percent of the synthetic budget to consume before reset |
| `timezone` | string | optional | IANA timezone for reset windows |
| `window_budget_usd` | number | optional | Synthetic per-window budget used for pacing and forecasting |



### `ralphglasses_automation_queue`

Manage the durable queue for repo-local subscription-window automation.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repo name |
| `action` | string | optional | Action: list, enqueue, remove, reprioritize (default: list) |
| `budget_usd` | number | optional | Budget override for queued item |
| `criteria` | string | optional | Comma-separated success criteria for cycle jobs |
| `id` | string | optional | Queue item ID for remove/reprioritize |
| `job_kind` | string | optional | Queue job kind: session (default), loop, cycle, research |
| `max_tasks` | number | optional | Max tasks for cycle jobs |
| `max_turns` | number | optional | Max turns override for queued item |
| `model` | string | optional | Model override for queued item |
| `name` | string | optional | Cycle name for cycle jobs |
| `objective` | string | optional | Cycle or research objective/topic |
| `priority` | number | optional | Queue priority (higher runs first) |
| `prompt` | string | optional | Prompt to enqueue |
| `provider` | string | optional | Provider override for queued item |
| `research_domain` | string | optional | Research domain for research jobs |
| `research_model_tier` | string | optional | Research queue model tier for research jobs |
| `research_priority_score` | number | optional | Research queue priority score for research jobs |
| `source` | string | optional | Queue item source label |



### `ralphglasses_autonomy_decisions`

Recent autonomous decisions with rationale, inputs, and outcomes

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `limit` | number | optional | Max decisions (default: 20) |



### `ralphglasses_autonomy_level`

View or set the autonomy level (0=observe, 1=auto-recover, 2=auto-optimize, 3=full-autonomy). When setting, also starts/stops the autonomous supervisor.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `level` | string | optional | New level: 0-3 or name (omit to view current) |
| `repo_path` | string | optional | Repo path for the supervisor (used when setting level) |



### `ralphglasses_autonomy_override`

Override/reverse an autonomous decision and record human intervention

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `decision_id` | string | **required** | Decision ID to override |
| `details` | string | optional | Why this was overridden |



### `ralphglasses_hitl_history`

Recent HITL events: manual stops, auto-recoveries, config changes, etc.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `hours` | number | optional | Time window in hours (default: 24) |
| `limit` | number | optional | Max events (default: 50) |



### `ralphglasses_hitl_score`

Current human-in-the-loop score: manual interventions vs autonomous actions, with trend

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `hours` | number | optional | Time window in hours (default: 24) |



### `ralphglasses_supervisor_status`

Returns the autonomous supervisor status including whether it's running, tick count, and last cycle launch time.

*No parameters.*




## workflow

Workflow automation — define, run, and delete multi-step YAML workflows that sequence agent sessions


### `ralphglasses_workflow_define`

Define a multi-step workflow as YAML

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Workflow name |
| `repo` | string | **required** | Repo name |
| `yaml` | string | **required** | Workflow YAML definition |



### `ralphglasses_workflow_delete`

Delete a workflow definition file

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Workflow name to delete |
| `repo` | string | **required** | Repo name |



### `ralphglasses_workflow_run`

Execute a defined workflow, launching sessions per step

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `name` | string | **required** | Workflow name |
| `repo` | string | **required** | Repo name |




## docs

Docs repo integration: search research, check existing, write findings, push changes, meta-roadmap coordination, cross-repo roadmap management


### `ralphglasses_docs_check_existing`

Check if research exists on a topic before starting new work. Searches SEARCH-GUIDE.md and all research files. Returns recommendation: read existing or proceed with new research.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `topic` | string | **required** | Topic to check (e.g., 'MCP tool design', 'cascade routing') |



### `ralphglasses_docs_push`

Commit and push all pending changes in the docs repo via push-docs.sh

*No parameters.*



### `ralphglasses_docs_search`

Full-text search across docs/research/ files using ripgrep. Returns matching file paths, line numbers, and previews.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `query` | string | **required** | Search query (regex supported) |
| `domain` | string | optional | Filter to research domain: mcp, agents, orchestration, cost-optimization, go-ecosystem, terminal, competitive |
| `limit` | number | optional | Max results (default: 20) |



### `ralphglasses_docs_write_finding`

Write a research finding to docs/research/<domain>/<filename>. Validates domain and creates directory if needed.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `content` | string | **required** | Markdown content to write |
| `domain` | string | **required** | Research domain: mcp, agents, orchestration, cost-optimization, go-ecosystem, terminal, competitive |
| `filename` | string | **required** | Filename (kebab-case.md, e.g., 'fleet-metrics-scaling.md') |



### `ralphglasses_meta_roadmap_next_task`

Get the next incomplete task from META-ROADMAP.md, optionally filtered by phase name

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `phase` | string | optional | Filter to phase containing this string (e.g., 'Wave 1', 'Security') |



### `ralphglasses_meta_roadmap_status`

Parse docs/strategy/META-ROADMAP.md and return phase count, task totals, completion percentage, and summary

*No parameters.*



### `ralphglasses_roadmap_assign_loop`

Create an R&D loop targeting a specific roadmap task. Returns loop_start parameters for the task.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | **required** | Repository name |
| `task` | string | **required** | Task description from roadmap |
| `budget_usd` | number | optional | Budget in USD (default: 3.0) |
| `provider` | string | optional | Provider: claude (default), gemini, codex |



### `ralphglasses_roadmap_cross_repo`

Compare roadmap progress across all repos using docs/snapshots/roadmaps/. Returns repos sorted by completion (least complete first).

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `limit` | number | optional | Max repos to return (default: 10) |




## recovery

Emergency session recovery: triage killed sessions, salvage partial output, generate recovery plans, batch re-launch, write incident reports, discover orphaned sessions


### `ralphglasses_incident_report`

Generate a structured incident report in docs/research/incidents/. Includes timeline, affected sessions, salvaged outputs, recovery actions taken, and lessons learned.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `title` | string | **required** | Incident title (kebab-case, used as filename) |
| `cause` | string | optional | Root cause description (e.g., 'hyprland-hy3-plugin-crash') |
| `recovery_sweep_id` | string | optional | Sweep ID from recovery_execute, if recovery was run |
| `session_ids` | string | optional | Comma-separated affected session IDs (omit to auto-discover) |
| `since` | string | optional | Incident window start (RFC3339 or relative). Default: 1h |



### `ralphglasses_recovery_execute`

Execute a recovery plan: batch re-launch retry sessions in parallel, tracked as a sweep with budget cap.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `plan_json` | string | **required** | JSON recovery plan (the 'actions' array from recovery_plan output) |
| `budget_cap_usd` | number | optional | Total sweep budget cap (default: 50.0) |
| `concurrency` | number | optional | Max simultaneous re-launches (default: 5) |
| `model_override` | string | optional | Override model for all retries (e.g., downgrade to save cost) |



### `ralphglasses_recovery_plan`

Generate a prioritized recovery plan from killed sessions. Categorizes each: retry (transient error), salvage-and-close (non-recoverable), or escalate (unclear). Respects budget cap.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `budget_cap_usd` | number | optional | Max total budget for retry operations (default: 50.0) |
| `session_ids` | string | optional | Comma-separated session IDs (omit to auto-discover from time window) |
| `since` | string | optional | Time window for auto-discovery (RFC3339 or relative). Default: 1h |
| `strategy` | string | optional | Strategy: conservative, aggressive, cost-aware (default: cost-aware) |



### `ralphglasses_session_discover`

Scan all repos' .ralph/ directories and Claude Code project dirs to discover session state beyond the local store. Finds orphaned processes and external session files.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `check_processes` | boolean | optional | Check if discovered sessions still have running processes (default: true) |
| `include_claude_projects` | boolean | optional | Also scan ~/.claude/projects/ for session metadata (default: true) |
| `scan_path` | string | optional | Base directory to scan (default: configured scan path) |



### `ralphglasses_session_salvage`

Extract partial output from a killed session, classify what was accomplished vs lost, and generate a recovery task prompt.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | string | **required** | Session ID to salvage |
| `generate_prompt` | boolean | optional | Generate a recovery prompt that continues where the session left off (default: true) |
| `save_to_docs` | string | optional | Domain to save salvaged findings to docs/research/<domain>/ (omit to skip) |



### `ralphglasses_session_triage`

Triage killed/interrupted sessions across all repos within a time window. Groups by kill reason, repo, provider. Shows cost wasted and recovery potential.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `repo` | string | optional | Filter by repo name (omit for all) |
| `since` | string | optional | Start of time window (RFC3339 or relative: '2h', '30m', '1d'). Default: 1h |
| `status` | string | optional | Comma-separated statuses: interrupted, errored, stopped (default: interrupted,errored) |
| `until` | string | optional | End of time window (RFC3339 or relative). Default: now |




## promptdj

Prompt DJ: quality-aware prompt routing to optimal providers


### `ralphglasses_promptdj_dispatch`

Route AND launch a session. Optionally enhances prompt if quality is low.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | Prompt to route and dispatch |
| `repo` | string | **required** | Repository path |
| `budget_usd` | number | optional | Max session budget USD |
| `dry_run` | boolean | optional | Preview routing without launching |
| `enhance` | string | optional | Enhancement: none, local, auto (default: auto) |
| `task_type` | string | optional | Override task type |



### `ralphglasses_promptdj_feedback`

Record outcome feedback to improve routing over time.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `decision_id` | string | **required** | Decision ID from route/dispatch |
| `success` | boolean | **required** | Whether session succeeded |
| `correct_provider` | string | optional | Correct provider if DJ chose wrong |
| `cost_usd` | number | optional | Actual cost USD |
| `notes` | string | optional | Outcome notes |
| `turns` | number | optional | Actual turn count |



### `ralphglasses_promptdj_history`

View routing decision history with optional summary. Filter by repo, provider, task type, status, and time window.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `limit` | number | optional | Max results (default 50) |
| `provider` | string | optional | Filter by provider |
| `repo` | string | optional | Filter by repo |
| `since` | string | optional | Time window: RFC3339 or duration ('24h', '7d') |
| `status` | string | optional | Filter: routed, dispatched, succeeded, failed, all |
| `summary` | boolean | optional | If true, return aggregate stats instead of individual decisions |
| `task_type` | string | optional | Filter by task type |



### `ralphglasses_promptdj_route`

Route a prompt to the best provider based on quality, task type, and domain. Does NOT launch a session.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | Prompt text to route |
| `repo` | string | optional | Repository name |
| `score` | number | optional | Pre-computed quality score 0-100 |
| `task_type` | string | optional | Override: code, analysis, troubleshooting, creative, workflow, general |



### `ralphglasses_promptdj_similar`

Find similar high-quality prompts from the registry for few-shot context injection. Uses BM25-lite keyword similarity, Jaccard tag overlap, and MMR diversity re-ranking.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | Prompt to find similar examples for |
| `repo` | string | optional | Repository context for relevance boosting |



### `ralphglasses_promptdj_suggest`

Get routing-aware improvement suggestions for a prompt. Shows where it would route, quality score, and actionable suggestions by category (quality, structure, cost, provider_fit).

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `prompt` | string | **required** | Prompt to analyze |
| `repo` | string | optional | Repository context |




## a2a

A2A protocol integration: discover agents, send tasks, check status, export agent card


### `ralphglasses_a2a_agent_card`

Generate and return this server's A2A agent card (our capabilities as an A2A agent).

*No parameters.*



### `ralphglasses_a2a_discover`

Discover an A2A agent by fetching its agent card from /.well-known/agent.json. Returns skills, capabilities, and metadata.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `url` | string | **required** | Base URL of the A2A agent |



### `ralphglasses_a2a_send`

Send a task to a remote A2A agent. Returns task ID and initial state.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `message` | string | **required** | Task message to send |
| `url` | string | **required** | Base URL of the A2A agent |
| `task_id` | string | optional | Optional task ID (auto-generated if empty) |



### `ralphglasses_a2a_status`

Check the status of a previously sent A2A task.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `task_id` | string | **required** | Task ID to check |
| `url` | string | **required** | Base URL of the A2A agent |




## trigger

External agent triggering and cron-based scheduling


### `ralphglasses_schedule_create`

Create, list, enable, or disable cron-based schedules. When repo is provided, schedules are persisted to that repo's .ralph/schedules/*.json; otherwise the legacy ~/.ralph/schedules.json store is used.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | optional | Action: create (default), list, enable, disable |
| `agent_type` | string | optional | Agent type: ralph (default), loop, cycle |
| `budget_usd` | number | optional | Budget override for repo-local schedules |
| `criteria` | string | optional | Comma-separated cycle success criteria |
| `cron_expression` | string | optional | Cron expression e.g. '0 */6 * * *' (required for create) |
| `enabled` | boolean | optional | Whether the schedule is active (default: true) |
| `id` | string | optional | Schedule ID (required for enable/disable) |
| `max_tasks` | number | optional | Max planned tasks for cycle schedules |
| `max_turns` | number | optional | Max turns override for repo-local schedules |
| `model` | string | optional | Model override for repo-local schedules |
| `name` | string | optional | Cycle name for cycle schedules |
| `objective` | string | optional | Cycle objective override for cycle schedules |
| `priority` | number | optional | Queue priority for repo-local schedules |
| `prompt` | string | optional | Task prompt (required for create) |
| `provider` | string | optional | Provider override for repo-local schedules |
| `repo` | string | optional | Repo name or path for canonical repo-local schedules |



### `ralphglasses_trigger_webhook`

Trigger an agent session from an external source (webhook, API, or another agent). Creates a trigger record and optionally launches immediately.

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `agent_type` | string | **required** | Agent type to trigger |
| `prompt` | string | **required** | Task prompt for the agent session |
| `budget_usd` | number | optional | Budget in USD for the session |
| `launch` | boolean | optional | Launch the session immediately (default: false, just queue) |
| `max_turns` | number | optional | Maximum conversation turns |
| `model` | string | optional | Model override for the session |
| `priority` | number | optional | Priority 1-10, higher = more urgent (default: 5) |
| `repo` | string | optional | Repo name (required when launch=true) |
| `tenant_id` | string | optional | Workspace tenant ID (default: _default) |




## approval

Human-in-the-loop approval: request, resolve, list pending approvals (Factor 7: Contact Humans with Tool Calls)


### `ralphglasses_list_approvals`

List pending approval requests (set include_resolved=true for all)

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `include_resolved` | boolean | optional | Include already-resolved approvals (default: false) |



### `ralphglasses_request_approval`

Request human approval for an action — creates a pending record and optionally pauses the session until resolved

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `action` | string | **required** | What needs approval (e.g. 'merge PR #42', 'deploy to prod') |
| `context` | string | **required** | Why this needs approval — background and rationale |
| `urgency` | string | **required** | Urgency level: low, normal, high, critical |
| `session_id` | string | optional | Session ID to pause while awaiting approval (omit to skip pause) |



### `ralphglasses_resolve_approval`

Resolve a pending approval — approves or rejects the requested action and resumes the paused session if applicable

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `approval_id` | string | **required** | Approval ID returned from request_approval |
| `decision` | string | **required** | Decision: approved or rejected |
| `reason` | string | optional | Reason for the decision |




## context

Context window budget monitoring: track token usage per session


### `ralphglasses_context_budget`

Get context window budget status for a session or all sessions. Returns used tokens, limit, utilization percent, and threshold status (ok/warning/critical).

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `session_id` | string | optional | Session ID (omit to return all sessions) |




## prefetch

Deterministic context pre-fetching: inspect registered hooks


### `ralphglasses_prefetch_status`

List registered prefetch hooks that run before session launch to pre-gather context

*No parameters.*



