run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - gosimple
    - staticcheck
    - unused
    - ineffassign
    - typecheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
      text: "fmt\\.Fprintf|fmt\\.Fprint"
