# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the repo-triage workflow"
prompt = """
Follow the Ralphglasses workflow `repo-triage` from `.agents/workflows/repo-triage.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///{repo}/triage, ralph:///{repo}/status, ralph:///{repo}/progress, ralph:///{repo}/logs, ralph:///runtime/health.
- Route the task through the matching skills: ralphglasses-repo-admin, ralphglasses-recovery-observability.
- Load only the required tool groups: core, repo, observability.
- Use the key tools as the primary execution path: ralphglasses_status, ralphglasses_repo_health, ralphglasses_logs.
- When structured prompting helps, use the matching prompts: repo-triage-brief, code-review, test-generation.
"""
