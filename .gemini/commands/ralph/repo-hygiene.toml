# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the repo-hygiene workflow"
prompt = """
Follow the Ralphglasses workflow `repo-hygiene` from `.agents/workflows/repo-hygiene.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///{repo}/triage, ralph:///catalog/workflows, ralph:///catalog/skills, ralph:///runtime/health.
- Route the task through the matching skills: ralphglasses-repo-admin, ralphglasses-discovery.
- Load only the required tool groups: core, repo, observability.
- Use the key tools as the primary execution path: ralphglasses_status, ralphglasses_repo_health, ralphglasses_worktree_list, ralphglasses_worktree_cleanup.
- When structured prompting helps, use the matching prompts: repo-triage-brief.
"""
