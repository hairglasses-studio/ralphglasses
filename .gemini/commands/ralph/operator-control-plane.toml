# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the operator-control-plane workflow"
prompt = """
Follow the Ralphglasses workflow `operator-control-plane` from `.agents/workflows/operator-control-plane.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///runtime/operator, ralph:///runtime/sessions, ralph:///bootstrap/checklist, ralph:///runtime/health.
- Route the task through the matching skills: ralphglasses-operator, ralphglasses-session-ops.
- Load only the required tool groups: management, fleet, session.
- Use the key tools as the primary execution path: ralphglasses_fleet_runtime, ralphglasses_marathon, ralphglasses_server_health, ralphglasses_session_launch.
"""
