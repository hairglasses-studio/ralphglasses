# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the provider-parity workflow"
prompt = """
Follow the Ralphglasses workflow `provider-parity` from `.agents/workflows/provider-parity.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///catalog/provider-parity, ralph:///catalog/server, ralph:///catalog/tool-groups, ralph:///catalog/skills, ralph:///catalog/cli-parity, ralph:///catalog/discovery-adoption, ralph:///catalog/adoption-priorities, ralph:///catalog/autobuild-tranches.
- Route the task through the matching skills: ralphglasses-self-dev, ralphglasses-discovery.
- Load only the required tool groups: repo, roadmap, docs.
- Use the key tools as the primary execution path: ralphglasses_server_health, ralphglasses_skill_export, ralphglasses_roadmap_analyze.
- When structured prompting helps, use the matching prompts: provider-parity-audit.
"""
