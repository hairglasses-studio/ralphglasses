# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the bootstrap-and-firstboot workflow"
prompt = """
Follow the Ralphglasses workflow `bootstrap-and-firstboot` from `.agents/workflows/bootstrap-and-firstboot.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///catalog/server, ralph:///catalog/skills, ralph:///bootstrap/checklist, ralph:///runtime/operator, ralph:///runtime/recovery.
- Route the task through the matching skills: ralphglasses-bootstrap, ralphglasses-operator.
- Load only the required tool groups: core, repo, tenant.
- Use the key tools as the primary execution path: ralphglasses_scan, ralphglasses_repo_scaffold.
- When structured prompting helps, use the matching prompts: bootstrap-firstboot.
"""
