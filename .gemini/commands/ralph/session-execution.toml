# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the session-execution workflow"
prompt = """
Follow the Ralphglasses workflow `session-execution` from `.agents/workflows/session-execution.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///runtime/sessions, ralph:///runtime/health, ralph:///catalog/skills.
- Route the task through the matching skills: ralphglasses-session-ops.
- Load only the required tool groups: session, core.
- Use the key tools as the primary execution path: ralphglasses_session_launch, ralphglasses_session_list, ralphglasses_session_status, ralphglasses_session_budget, ralphglasses_session_handoff.
"""
