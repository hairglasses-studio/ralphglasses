# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the discover-and-load workflow"
prompt = """
Follow the Ralphglasses workflow `discover-and-load` from `.agents/workflows/discover-and-load.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///catalog/server, ralph:///catalog/tool-groups, ralph:///catalog/skills, ralph:///catalog/autobuild-tranches.
- Route the task through the matching skills: ralphglasses-discovery.
- Load only the required tool groups: core.
- Use the key tools as the primary execution path: ralphglasses_server_health, ralphglasses_tool_groups, ralphglasses_load_tool_group.
"""
