# GENERATED BY tools/genskillsurface; DO NOT EDIT
description = "Run the runtime-recovery workflow"
prompt = """
Follow the Ralphglasses workflow `runtime-recovery` from `.agents/workflows/runtime-recovery.md`.

Requirements:
1. Read `AGENTS.md` and `.agents/rules/ralphglasses.md` before mutating the repo.
2. Use the workflow references and repo-native skills instead of inventing parallel flows.
3. Keep the response concise, source-backed, and aligned with the MCP contract.

Workflow summary:
- Start from the repo-native references before using mutating tools.
- Read the relevant resources first: ralph:///runtime/recovery, ralph:///runtime/health, ralph:///catalog/skills, ralph:///{repo}/logs.
- Route the task through the matching skills: ralphglasses-bootstrap, ralphglasses-recovery-observability.
- Load only the required tool groups: core, recovery, observability, fleet.
- Use the key tools as the primary execution path: ralphglasses_server_health, ralphglasses_logs, ralphglasses_recovery_plan, ralphglasses_session_triage.
"""
