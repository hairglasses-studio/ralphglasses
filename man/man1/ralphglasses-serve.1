.nh
.TH "RALPHGLASSES" "1" "Apr 2026" "ralphglasses" ""

.SH NAME
ralphglasses-serve - Run as a fleet node (coordinator or worker)


.SH SYNOPSIS
\fBralphglasses serve [flags]\fP


.SH DESCRIPTION
Start ralphglasses as a fleet node for distributed workload management.

.PP
Coordinator mode (one per fleet):
  ralphglasses serve --coordinator --port 9473

.PP
Worker mode (N per fleet):
  ralphglasses serve --coordinator-url http://100.x.y.z:9473

.PP
If --coordinator-url is not specified in worker mode, the node will probe
Tailscale peers on the fleet port to auto-discover the coordinator.


.SH OPTIONS
\fB--automation\fP[=true]
	Run repo-local subscription automation supervisors alongside fleet serving

.PP
\fB--coordinator\fP[=false]
	Run as fleet coordinator (default: worker)

.PP
\fB--coordinator-url\fP=""
	Coordinator URL (auto-discover via Tailscale if empty)

.PP
\fB--fleet-budget\fP=500
	Fleet-wide budget ceiling in USD

.PP
\fB-h\fP, \fB--help\fP[=false]
	help for serve

.PP
\fB--port\fP=9473
	HTTP port for fleet communication


.SH OPTIONS INHERITED FROM PARENT COMMANDS
\fB--debug\fP[=false]
	Enable verbose debug logging to stderr

.PP
\fB--http-addr\fP=""
	Enable health endpoints on this address (e.g. :9090, disabled by default)

.PP
\fB--log-format\fP="json"
	Log format (json, text)

.PP
\fB--log-level\fP="info"
	Log level (debug, info, warn, error)

.PP
\fB--scan-path\fP="~/hairglasses-studio"
	Root directory to scan for ralph-enabled repos

.PP
\fB--scan-timeout\fP="30s"
	Timeout for repository scanning (e.g. 30s, 1m)

.PP
\fB--web-addr\fP=""
	Enable web UI on this address (e.g. :8080, disabled by default). Set RALPHGLASSES_WEB_TOKEN for auth.


.SH SEE ALSO
\fBralphglasses(1)\fP


.SH HISTORY
8-Apr-2026 Auto generated by spf13/cobra
