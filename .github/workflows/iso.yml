name: ISO Build

on:
  push:
    branches: [main]
    paths:
      - 'distro/**'

permissions:
  contents: read

concurrency:
  group: iso-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build-iso:
    uses: ./.github/workflows/reusable-iso-build.yml
