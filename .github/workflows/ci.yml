name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: hairglasses-studio/.github/.github/workflows/go-test.yml@main
    with:
      go-version: "1.26.1"
      test-flags: "-count=1"
      coverage-threshold: 0
